<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <link rel="stylesheet" href="../public/browser.css"/>
    <style>
        .container{
            width: 80vw;
            margin: auto;
        }
        .todo-div{
            display: flex;
            align-items: center;
            justify-content: center;
            background-color: lightgray;
            /* width: 80%; */
            margin: auto;
            padding: 0.8rem;
            gap: 1rem;
        }
        input{
            width: 80%;
            padding: 1rem 0.8rem;
            border:1px solid black;
            font-size: 1rem;
            border-radius: 0.7rem;
        }
        input:focus{
            outline: none;
        }
        .add-btn{
            background-color: blue;
            color: white;
            padding: 1rem 0.8rem;
            cursor: pointer;
            font-size: 1rem;
            border-radius: 0.7rem;
            border: none;
        }
        .logout-btn, .show-more{
            background-color: blue;
            color: white;
            padding: 1rem;
            cursor: pointer;
            border: none;
            font-weight: 600;
            font-size: 1rem;
            border-radius: 0.7rem;
        }
        .logout-btn:hover,.show-more:hover{
            cursor: pointer;
            box-shadow: 2px 2px 2px black;
            transition: all 0.3s;
        }
        .logout-form{
            display: flex;
            align-items: flex-start;
            margin: 2rem 0rem;
        }
        /* styles for ul */
        ul{
            list-style: none;
            padding: 0;
            margin: 3rem 0rem;
            font-family: 'Lucida Sans', 'Lucida Sans Regular', 'Lucida Grande', 'Lucida Sans Unicode', Geneva, Verdana, sans-serif;
        }
        .todo{
          display: flex;  
          justify-content: space-between;
          align-items: center;
          padding: 1rem 1.5rem;
          border: 1px solid rgb(55, 55, 55);
          border-radius: 0.5rem;
          margin: 1.5rem 0rem;
          box-shadow: 0px 0px 2px rgb(52, 52, 52);
        }
        span{
            font-size: 1rem;
        }
        .btn{
            background-color: blue;
            padding:0.5rem 1rem;
            border-radius: 0.5rem;
            border: none;
            color: white;
            font-weight: 600;
            font-size: 0.8rem;
        }
        .btn:hover{
            cursor: pointer;
            box-shadow: 1px 1px 5px black;
            transition: all 0.3s;
        }
        .delete-btn{
            background-color: red;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1 style="text-align: center; font-family: 'Lucida Sans', 'Lucida Sans Regular', 'Lucida Grande', 'Lucida Sans Unicode', Geneva, Verdana, sans-serif;">
            Welcome to the ToDo Dashboard
        </h1>
        <!-- <form class="todo-form" method="post" action="/create-item"> -->
            <div class="todo-div">
                <input id="todo-input" type="text" name="todo" placeholder="add a todo">
                <button class="add-btn" type="submit">Add New Item</button>
            </div>
        <!-- </form> -->
        <ul id="item-list"></ul>
        <!-- show more btn -->
         <button class="show-more">Show More</button>
         <br>
         <br>
        <!-- <form method="post" action="/logout" class="logout-form"> -->
            <button class="logout-btn" type="submit">Logout</button>
        <!-- </form> -->
    </div>
    <script src="https://unpkg.com/axios@1.6.7/dist/axios.min.js"></script>
    <script src="/browser.js"></script>
</body>
</html>